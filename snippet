        String fileName = "C:\\Users\\Desktop\\En-Ch Longman Dictionary of Contemporary English.txt";
        String resultName = "C:\\Users\\Desktop\\result.txt";

        List<String> allLines = Files.readAllLines(Paths.get(fileName));

        List<String> resultList = new ArrayList<>();

        int count = 0;
        for (String line : allLines) {
            if (line.indexOf("。") != -1) {
                String[] en = line.split(".  ");
                resultList.add(en[0]);
            }

        }

        Files.write(Paths.get(resultName), resultList);
